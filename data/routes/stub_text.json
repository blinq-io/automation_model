{
  "template": "The user navigates through various links and buttons on the httpbin.org page",
  "routes": [
    {
      "filters": {
        "path": "/html",
        "method": "GET",
        "queryParams": {}
      },
      "actions": [
        {
          "type": "stub_request",
          "config": {
            "statusCode": "200",
            "body": "BVT Analysis Formatter",
            "contentType": "text/html; charset=utf-8"
          }
        }
      ],
      "mandatory": true,
      "timeout": 5000
    }
  ]
}
